package trivial.rest

import org.scalamock.scalatest.MockFactory
import org.scalatest.{MustMatchers, WordSpec}

class MigrationSpec extends WordSpec with MustMatchers with MockFactory {
  "We can specify a migration script which converts an old data format into a new format" in {
    pending
  }
}